use pip3 to install the required libraries first

command: pip3 install -r [path-to-req.txt]

app.py is the backend. webpage.html is the frontend.
the jupyter notebooks are the training and the testing code for the ai model.